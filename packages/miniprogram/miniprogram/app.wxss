/**
 * 全局样式 - vibecard Light Mode
 * Design System: 明亮、极简、高级感、年轻活力
 */

/* ==================== 基础设置 ==================== */
view, scroll-view, text, image, button, input, textarea, navigator {
  box-sizing: border-box;
}

page {
  /* 背景 - 硅谷风浅灰底 */
  --color-bg: #f7f7f8;
  --color-bg-elevated: #ffffff;
  --color-bg-card: #ffffff;
  --color-surface: #f1f1f4;
  --color-border: #eaeaee;
  --color-border-light: #f1f1f4;

  /* 文本 */
  --color-text: #16161a;
  --color-text-secondary: #6b6b72;
  --color-text-tertiary: #a8a8b0;

  /* 主色 - indigo 单强调 */
  --color-primary: #6366f1;
  --color-primary-dark: #4f46e5;
  --color-primary-light: #eceefe;
  --color-primary-soft: #f4f4fe;

  /* 强调色 - 与主色统一 */
  --color-accent: #6366f1;
  --color-accent-dark: #4f46e5;
  --color-accent-soft: rgba(99, 102, 241, 0.08);

  /* 圆角 - 增大以匹配 Web 端 */
  --radius-sm: 16rpx;
  --radius-md: 24rpx;
  --radius-lg: 32rpx;
  --radius-xl: 40rpx;
  --radius-full: 100rpx;

  /* 间距 */
  --space-xs: 8rpx;
  --space-sm: 16rpx;
  --space-md: 24rpx;
  --space-lg: 32rpx;
  --space-xl: 48rpx;

  /* 阴影 - 更柔和，对齐 Web 端 */
  --shadow-sm: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);
  --shadow-md: 0 4rpx 24rpx rgba(0, 0, 0, 0.05);
  --shadow-lg: 0 8rpx 32rpx rgba(0, 0, 0, 0.06);
  --shadow-glow: 0 4rpx 20rpx rgba(0, 0, 0, 0.08);

  background: var(--color-bg);
  font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'PingFang SC', 'Noto Sans SC', 'Source Han Sans SC', sans-serif;
  font-size: 28rpx;
  color: var(--color-text);
  line-height: 1.5;
  min-height: 100vh;
  -webkit-font-smoothing: antialiased;
}

/* ==================== 安全区域 ==================== */
.safe-bottom {
  padding-bottom: calc(120rpx + env(safe-area-inset-bottom));
}

/* ==================== 文字系统 ==================== */
.text-display {
  font-size: 64rpx;
  font-weight: 700;
  letter-spacing: -1rpx;
  line-height: 1.2;
}

.text-title {
  font-size: 40rpx;
  font-weight: 600;
  letter-spacing: -0.5rpx;
  line-height: 1.3;
}

.text-headline {
  font-size: 32rpx;
  font-weight: 600;
  line-height: 1.4;
}

.text-body {
  font-size: 28rpx;
  font-weight: 400;
  line-height: 1.6;
}

.text-caption {
  font-size: 24rpx;
  font-weight: 400;
  line-height: 1.5;
  color: var(--color-text-secondary);
}

.text-label {
  font-size: 22rpx;
  font-weight: 500;
  letter-spacing: 1rpx;
  color: var(--color-text-tertiary);
}

/* 文字颜色 */
.text-primary { color: var(--color-text); }
.text-secondary { color: var(--color-text-secondary); }
.text-tertiary { color: var(--color-text-tertiary); }

/* ==================== 卡片系统 ==================== */
.card {
  background: var(--color-bg-card);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-sm);
  border: 1rpx solid var(--color-border);
}

.card-elevated {
  background: var(--color-bg-elevated);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-md);
  border: 1rpx solid var(--color-border);
}

.card-accent {
  background: linear-gradient(135deg, rgba(0, 0, 0, 0.03) 0%, rgba(0, 0, 0, 0.01) 100%);
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-glow);
  border: 1rpx solid rgba(0, 0, 0, 0.06);
}

/* ==================== 按钮系统 ==================== */
.btn {
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 500;
  border-radius: var(--radius-full);
  transition: all 0.2s ease;
}

.btn-primary {
  height: 100rpx;
  padding: 0 48rpx;
  background: #0a0a0a;
  color: #fff;
  font-size: 30rpx;
  font-weight: 600;
}

.btn-primary:active {
  transform: scale(0.98);
  opacity: 0.85;
}

.btn-secondary {
  height: 88rpx;
  padding: 0 40rpx;
  background: #fff;
  color: #0a0a0a;
  font-size: 28rpx;
  border: 1rpx solid #e8e8e8;
  border-radius: var(--radius-full);
}

.btn-secondary:active {
  background: #f5f5f5;
}

/* ==================== 标签系统 ==================== */
.tag {
  display: inline-flex;
  align-items: center;
  padding: 8rpx 20rpx;
  background: #f5f5f5;
  border-radius: var(--radius-full);
  font-size: 22rpx;
  color: var(--color-text-secondary);
  border: 1rpx solid #f0f0f0;
}

.tag-active {
  background: #0a0a0a;
  color: #fff;
  border-color: #0a0a0a;
}

/* ==================== 动画 ==================== */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(20rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.6; }
}

@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10rpx); }
}

@keyframes glow {
  0%, 100% { opacity: 0.6; }
  50% { opacity: 1; }
}

/* ==================== 工具类 ==================== */
.flex { display: flex; }
.flex-col { flex-direction: column; }
.flex-center { align-items: center; justify-content: center; }
.flex-between { justify-content: space-between; }
.flex-1 { flex: 1; }

.w-full { width: 100%; }
.h-full { height: 100%; }

.rounded-full { border-radius: 50%; }

/* ==================== TabBar 占位 ==================== */
.tabbar-placeholder {
  height: calc(120rpx + env(safe-area-inset-bottom));
}

/* ==================== 隐私协议弹窗 ==================== */
.privacy-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.4);
  z-index: 9999;
  display: flex;
  align-items: center;
  justify-content: center;
}

.privacy-dialog {
  width: 580rpx;
  background: #ffffff;
  border-radius: 24rpx;
  padding: 48rpx 40rpx;
  box-shadow: 0 16rpx 48rpx rgba(0, 0, 0, 0.12);
  border: 1rpx solid #f0f0f0;
}

.privacy-title {
  font-size: 34rpx;
  font-weight: 600;
  color: #0a0a0a;
  text-align: center;
  margin-bottom: 24rpx;
}

.privacy-content {
  font-size: 28rpx;
  color: #737373;
  line-height: 1.8;
  text-align: center;
  margin-bottom: 40rpx;
}

.privacy-link {
  color: #0a0a0a;
  font-weight: 500;
}

.privacy-buttons {
  display: flex;
  gap: 24rpx;
}

.privacy-btn {
  flex: 1;
  height: 80rpx;
  border-radius: 40rpx;
  font-size: 28rpx;
  font-weight: 500;
  display: flex;
  align-items: center;
  justify-content: center;
  border: none;
  line-height: 80rpx;
  padding: 0;
  margin: 0;
}

.privacy-btn::after {
  border: none;
}

.privacy-btn.reject {
  background: #f5f5f5;
  color: #737373;
}

.privacy-btn.agree {
  background: #0a0a0a;
  color: #fff;
}
