{
  "name": "@vibecard/server",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "private": true,
  "description": "Open self-hosted VibeCard server: owner agent, public Card, visitor agent, and connection requests over the portable Core + local SQLite store.",
  "scripts": {
    "start": "node --import tsx src/main.ts",
    "backup": "node --import tsx src/cli.ts backup",
    "restore": "node --import tsx src/cli.ts restore",
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "engines": {
    "node": ">=24"
  }
}
